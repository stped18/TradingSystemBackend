version: '3'
services:
  web-server:
    build:
      dockerfile: Dockerfile
    ports:
      - 5800:5800
    networks:
      - spring-cloud-network
networks:
  spring-cloud-network:
    driver: bridge